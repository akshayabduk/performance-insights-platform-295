import React from 'react';

// PUBLIC_INTERFACE
export default function Admin() {
  /** Admin tools and system configuration panel (placeholder). */
  return (
    <div className="card">
      <div className="card-title">Admin Panel</div>
      <p>Manage users, roles, and system settings. (Feature placeholder)</p>
    </div>
  );
}
